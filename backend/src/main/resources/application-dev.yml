# 开发环境配置 - 连接本地 MySQL/Redis
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/plan_system?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: root123
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.plansystem: debug
